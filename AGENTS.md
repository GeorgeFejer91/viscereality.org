# Repository Guidance For AI Agents

Before making changes in this repository, read:

```text
for-ai/README.md
for-ai/constraints.md
for-ai/subprojects.md
```

For PowerPoint-to-HTML presentation work, also read:

```text
for-ai/pptx-html-one-to-one-mapper.md
for-ai/pptx-html-family-status.md
```

Keep the `for-ai/` notes current. When a task changes durable project constraints, build workflow, QA status, asset policy, or known blockers, update the relevant `for-ai` file in the same change.
